import styled from '@emotion/styled';
import { PageMargin } from '../styles/PageMargin';

export const MainPageMargin = PageMargin;

export const MainContent = styled.div`
  min-height: calc(100vh - 10rem);
  height: 100%;
  padding: 1.5rem 0;
`;
